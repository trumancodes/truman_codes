import '../styles/utility/reset.css';
import '../styles/utility/global.scss';
import '../styles/utility/swiper.scss';

export default function App({ Component, pageProps }) {
  return (
    <Component {...pageProps} />
  );
}